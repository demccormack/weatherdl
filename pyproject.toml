[project]
name = "weatherdl"
version = "v1.4.0"
description = "Weather download tool"
readme = "README.md"
dependencies = [
    "filetype==1.2.0",
    "python-pptx==1.0.2",
    "pytz==2026.2",
    "requests==2.33.1",
]

[project.optional-dependencies]
dev = [
    "black==26.3.1",
    "pylint==4.0.5",
    "pytest==9.0.3",
    "isort==8.0.1",
    "autopep8==2.3.2",
]
build = [
    "pyinstaller==6.20.0",
]

[tool.uv]
required-version = "0.11.7"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]
pythonpath = "src"

[tool.pylint.'tests/*.py']
disable = ["import-error", "missing-function-docstring", "missing-module-docstring"]
